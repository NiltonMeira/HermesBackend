export type TQueue = {
    id: string,
    bussinesModelId: string,
    position: Number,
    partNumber: string,
    npk: Number,
    capacityBatch: Number,
    batchQuantity: Number,
    BodiesQuantity: Number
}